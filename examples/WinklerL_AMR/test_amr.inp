*include, input=mesh.inp

*material, name=GCDP, id=GCDP,
** E
25850,
** nu
0.18,
** fcy
10.33,
** fcu
31.0,
** fbu
35.96,
** ftu
2.70,
** Ah
0.85,
** Bh
0.08,
** Ch
0.003,
** Dh
2.0,
** Df
0.000001,
** As
15,
** epsF
0.0017,
** l
2,
** m
1.0,
** maxDMG
.99,
** derivativeMethod (0 ..  AD, 1 ..  CD)
0
** dtThreshild
1e-12
** viscoplasticity
1e-12

*material, name=linearElastic, id=steelLoad
37000.0, 0.3

*section, name=sectConc, material=GCDP, type=solid
concrete
*section, name=secSteel, material=steelLoad, type=solid
reinforced_concrete

*modelModifier, type=hAdaptivity, name=amr
** reactive: ZZ recovered-gradient error of the nonlocal damage field localizes on the process zone
** >>marker, type=recoveryError, nodeField='nonlocal damage', markFraction=0.5, maxRefinedFraction=0.1, halo=1
** predictive: refine ahead of the front where the local driving field (alphaP) starts to grow
** >>marker, type=fieldOutput, fieldOutput=alphaPForAMR, operator='>', threshold=0.1
** predictive: refine crack-nucleation sites where the max principal (Rankine) stress reaches 0.9*ftu.
** The quantity (max principal stress per quadrature point) is computed by the fieldOutput's f(x)
** below via eigVal; the marker only thresholds it (0.9*2.70 = 2.43). It marks the element if any
** quadrature point reaches the threshold.
>>marker, type=fieldOutput, fieldOutput=stressForAMR, operator='>=', threshold=2.43
elSet=concrete
maxLevel=1
splitFactor=3

*job, domain=3d, name=theJob
*solver, solver=NISTParallel, name=theSolver

*fieldOutput,
>>perNode, elSet=concrete, name=displacement, field=displacement, result=U
>>perNode, elSet=concrete, name=nonlocal damage, field=nonlocal damage, result=U
>>perNode, name=RF, nSet=nset_load_application, field=displacement, result=P, f(x)='np.sum(x[:,1], axis=0)', saveHistory=True, export=RF
>>perElement, elSet=concrete, name=damage, result=omega, quadraturePoint=0:8, f(x)='np.mean(x,axis=1)'
>>perElement, elSet=concrete, name=alphaP, result=alphaP, quadraturePoint=0:8, f(x)='np.min(x,axis=1)'
>>perElement, elSet=concrete, name=alphaPForAMR, result=alphaP, quadraturePoint=0:8
>>perElement, elSet=concrete, name=stressForAMR, result=stress, quadraturePoint=0:8, f(x)='eigVal(x.reshape(-1,6))[:,0].reshape(x.shape[0],x.shape[1])'

*output, type=monitor, name=monitor
fieldOutput=RF

*output, type=ensight, name=esExport
>>perNode, fieldOutput=displacement
>>perNode, fieldOutput=nonlocal damage
>>perElement, fieldOutput=damage
>>configuration, overwrite=yes

*step, solver=theSolver
maxInc=1e-2, minInc=1e-4, maxNumInc=200, maxIter=25, stepLength=100
>>options, name=theSolver, extrapolation=linear
>>dirichlet, name=bottom, nSet=nset_bottom, field=displacement, 2=0, 1=0, 3=0
>>dirichlet, name=load, nSet=nset_load_application, field=displacement, 2=+1
